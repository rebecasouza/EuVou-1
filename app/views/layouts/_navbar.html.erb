<nav class="orange darken-4" >
  <div class="nav-wrapper ">
    <a <%= active_menu('events') %>><%= link_to ".::Euvou::.", events_path %></a>


    <ul class="right hide-on-med-and-down">
      <% if user_signed_in? %>


          <li><a href="badges.html"><%= image_tag current_user.image,class: 'profile_img' %></a></li>



          <li>
            <a class="dropdown-button" href="#!" data-activates="dropdown1"><i class="material-icons right">arrow_drop_down</i>
              <%= current_user.email_verified? ? current_user.name : "Carregando..."   %>

              <%= current_user.identities.size == 0 ? "" : "(via " + current_user.identities.map(&:provider).join("/").titleize + ")" %>


            </a>
          </li>

          <!-- Dropdown Structure -->
          <ul id="dropdown1" class="dropdown-content ">

            <li>
              <% if current_user.identities.count == 0  %>
                  <%= link_to("Editar perfil", edit_user_registration_path) %>
              <% else %>
                  <%= link_to_provider(current_user) %>
              <% end %>
            </li>
            <li class="divider"></li>
            <li> <%= link_to "Sair", destroy_user_session_path, method: :delete %></li>

          </ul>


          <li><%= link_to 'Novo evento', new_event_path, class: "btn btn-default orange darken-3" %></li>
      <% else %>
          <li><%= link_to "Login", new_user_session_path %></li>
          <li><%= link_to "Cadastre-se", new_user_registration_path %></li>

      <%end %>
    </ul>

  </div>
</nav>


