<%= form_for(current_user.eu_vous.find_by(attended_event_id: @event.id), html: { method: :delete }) do |f| %>
<%= f.submit "Não vou", class: "btn btn-large", style:"width: 250px; background-color: #e65100;" %>
<% end %>